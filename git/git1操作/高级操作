vim的高级用法主要是版本回退和分支
首先是git commit -m ////git commit -a
这两个的区别是
-a不仅会提交在暂存区域的数据,还会提交已修改但是未暂存的数据,如果是如果是-m会不能提交,commit后会显示一个nano界面,写上详细注释,才可提交
将nano改为vim编辑器:命令行输入git config --global core.editor vim
如果不想直接进入编辑器中修改:git commit -m -a "some detail"

git 分支操作




gitk图形化界面的使用




git回退历史版本






































































































































